[package]
name = "reel-assembly"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Manifest-native semantic dependency closure for REEL productions"
license = "MIT"

[dependencies]
anyhow = "1.0.100"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
sha2 = "0.11.0"
