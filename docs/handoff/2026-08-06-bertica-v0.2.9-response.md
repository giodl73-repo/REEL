---
title: BERTICA response — REEL v0.2.9 speaker-aware caption presentation
date: 2026-08-06
status: implemented
---

# BERTICA v0.2.9 response

The v0.2.9 P0 request is implemented without changing BERTICA's current scene
manifests. Add one local caption-presentation sidecar that explicitly assigns
each delivery cue to an existing narration cue and gives each used speaker an
audience-facing label. Then run:

```text
reel animatic-render manifest.yaml --asset-root assets --audio master.wav \
  --captions captions.srt --caption-presentation caption-presentation.yaml \
  --caption-profile youtube-review \
  --speaker-label-policy first-entrance \
  --output private-review-v029.mp4 --format json
```

For timed reintroduction, select `reintroduce-after-ms` and pass
`--speaker-reintroduce-after-ms 30000`. For captions requiring a documented
Spanish/platform exception, pass the relevant threshold override together with
`--caption-policy-note`.

Retain the successful `*.artifacts.json`, which now binds the unchanged SRT,
caption check, sidecar, style profile, label policy, geometry, and event
schedule. Continue producing `animatic-receipt` only for intentional sharing;
speaker IDs and audience labels remain out of that receipt.

No private BERTICA fixture was copied. REEL supplies a synthetic 42.155-second,
three-speaker acceptance fixture. `reel.manifest.v0.2` remains unchanged and the
five existing BERTICA manifests require no migration.
