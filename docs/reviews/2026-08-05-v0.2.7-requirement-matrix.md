# v0.2.7 requirement matrix

| Requirement | Evidence |
| --- | --- |
| Recipient does not need the private artifact report | `animatic-receipt-check` accepts only receipt and video |
| Unknown or path fields fail closed | `deny_unknown_fields` plus adversarial CLI coverage |
| Wrong video fails | SHA-256 and byte-length comparison with tamper coverage |
| Delivery claims are measured | ffprobe checks codec, pixel format, dimensions, CFR, duration, and audio streams |
| Verification output stays private | Path-free report schema and CI forbidden-token scan |
| Integrity is not authorship | Trust boundary explicitly excludes signature, consent, release, and approval |
| Manifest stability | `reel.manifest.v0.2` remains unchanged; no migration |
