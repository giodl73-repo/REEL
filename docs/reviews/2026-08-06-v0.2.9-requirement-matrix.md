# v0.2.9 requirement matrix

| Requirement | Evidence |
| --- | --- |
| Four label policies | Unit coverage for none, first entrance, persistent, and 10-second reintroduction |
| Audience label differs from internal identity | Strict sidecar maps `speaker_id` to `audience_label`; synthetic fixture uses distinct values |
| Do not mutate dialogue | Six-policy/aspect test proves one invariant SRT hash and timeline |
| Deterministic 16:9 and 9:16 style | YouTube and phone profiles record font, contrast, margins, and disjoint pixel regions |
| Automatic caption gate | Every render reconstructs `caption-check` before FFmpeg/output publication |
| Record overrides | Non-default thresholds require and serialize a policy note |
| Never infer speaker | Every SRT index maps to a known narration cue; unknown speaker rejection is atomic |
| Artifact binding | v0.2.9+ `animatic-check` reconstructs SRT, sidecar, report, policy, style, and event hashes |
| Receipt privacy and verification | Real matrix creates/checks artifacts and receipts; labels and speaker IDs are absent from receipts |
| Acceptance fixture | Synthetic 42.155 seconds, three speakers, eight source cues, eleven delivery cues; six real renders |
| Manifest stability | `reel.manifest.v0.2` unchanged; no migration |
