# v0.2.3 operational hardening matrix

| Learning | Implementation | Verification |
| --- | --- | --- |
| Whole-video cadence hides shot intent | `motion-check` trims by manifest shot and distinguishes moving from hold treatments | Real pan and hold/hold-dark acceptance cases |
| Transform math can crop declared composition | Endpoint containment for canvas, focal point, and protected regions | Positive center and negative edge unit tests; lineage evidence |
| Single-shot memory does not bound a multi-shot graph | Concurrent perspective-filter count and 2048 MiB preflight | 720p and 1080p resource boundary unit tests |
| Newer FFmpeg rejects undefined-rate branches at `xfade` | Normalize every shot branch to the requested FPS and AV time base | Dry-run graph assertion and FFmpeg 7.1 real-render CI |
| A report is not proof after files change | `animatic-check` rehashes inputs/output and verifies streams, captions, timing, and lineage | Real render pass plus tampered-hash rejection |
| CI setup was slow and duplicated | Pinned FFmpeg 7.1 GPL build/action, capability probes, timeout, branch concurrency cancellation | Workflow syntax and real-FFmpeg job |
| Consumer schema stability | CLI 0.2.3; `MANIFEST_VERSION` remains `reel.manifest.v0.2` | Existing v0.2 fixture and full regression suite |
