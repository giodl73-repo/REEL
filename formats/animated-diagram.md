# Animated diagram grammar

An `animated-diagram` uses motion to expose state, causality, comparison, or
change over time. Motion must carry meaning rather than decorate a static chart.

Review sequencing, labels, color semantics, reduced-motion alternatives,
caption compatibility, and whether the final state remains understandable.
